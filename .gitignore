node_modules/
dist/
.git-submodule-*/
*.log
.DS_Store
# 构建期准备的运行时资源（含 node.exe 与 bl/bailian 私有依赖），不进 git
resources/node/
resources/bin/node_modules/
# 含凭证的本地配置文件，绝不进 git（.env.example 模板保留）
.env
# 网盘凭证加密主密钥（v0.1.27 起 AES-256-GCM 用），绝不可进 git，否则加密形同虚设
.masterkey
