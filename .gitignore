node_modules/
dist/
.git-submodule-*/
*.log
.DS_Store
# 构建期准备的运行时资源（含 node.exe 与 bl/bailian 私有依赖），不进 git
resources/node/
resources/bin/node_modules/
# 构建期从 ~/.workbuddy/skills 复制进来的夸克官方 Skill（体积大且非本仓库源码），不进 git
resources/quarkclouddrive/
# ⚠ vendor/quarkclouddrive/ 是**故意提交**的夸克 Skill 纯代码副本（601KB CLI + references + SKILL.md）
#   原因：CI 从 GitHub 全新 checkout，拿不到本机 ~/.workbuddy/skills，v2.8.31 曾因此出缺夸克引擎的残包。
#   仓库里这一份是唯一能保证 CI 打包完整的来源。**但以下运行时状态必须永远排除**：
vendor/quarkclouddrive/.quarkclouddrive/
vendor/quarkclouddrive/**/.quarkclouddrive/
vendor/quarkclouddrive/**/credentials*
vendor/quarkclouddrive/workbuddy/search/
vendor/quarkclouddrive/workbuddy/download/
vendor/quarkclouddrive/workbuddy/upload/
vendor/quarkclouddrive/workbuddy/config.json
# prune-bailian.js 的 npm 兜底逻辑在构建时生成的副产物，不进 git
# （只忽略这一个文件；resources/bin/bl.cmd 是必要文件，必须保持被 git 跟踪）
resources/bin/package.json
# 含凭证的本地配置文件，绝不进 git（.env.example 模板保留）
.env
# 网盘凭证加密主密钥（v0.1.27 起 AES-256-GCM 用），绝不可进 git，否则加密形同虚设
.masterkey
# 构建期内联生成的 renderer 临时文件（由 scripts/prepare-build.js 生成，不进 git）
renderer/style.inline.css
renderer/index.inline.html
# 机器特定的代理配置（用户本地 .proxy / proxy-default），绝不进 git
.proxy
proxy-default
**/.proxy
**/proxy-default
# 测试临时目录（kdocs-tool/test/.tmp-*，约定不写 %TEMP%/C 盘，测试结束自动清理；残留兜底忽略）
kdocs-tool/test/.tmp-*/
# 运行期本地积累的离线数据缓存（rememberAppId 写入，不进 git；内置 data-pack.json 才是数据源）
kdocs-tool/cache/
# Python 缓存（resolve-auto 等脚本目录）
__pycache__/
*.pyc
# 本地临时探测目录（会话调试残留，含临时 node_modules；约定只写 E 盘，不进 git）
.tmp/
# 内置 biliup.exe（11MB 外部二进制，CI 与 prepare:biliup-bin 会拉取，不进 git）
biliup-hub/bin/biliup.exe
