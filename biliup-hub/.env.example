# 复制本文件为 .env（一般无需手改；配置主要在 UI 内编辑并持久化到 data/config.json）
#
# v0.1.64 起：biliup.exe 路径不再由 UI 配置，改为按运行环境自动解析：
#   - 打包后：<resources>/biliup-hub/bin/biliup.exe（随安装包内置，由 CI 从 biliup-rs Release 下载）
#   - 开发期：回退 D:\biliupR\biliup.exe 方便本地调试
# cookies.json 路径 = BILIUP_DATA_DIR/cookies.json（由工具箱主进程注入；开发期回退安装目录 data/）。
# 以下变量仅供说明，server.js 实际不再读取 BILIUP_EXE_PATH / COOKIES_PATH。

# （已废弃）biliup.exe 路径，现自动解析
# BILIUP_EXE_PATH=D:\biliupR\biliup.exe

# （已废弃）cookies.json 路径，现由 BILIUP_DATA_DIR 决定
# COOKIES_PATH=D:\biliupR\cookies.json

# 本地服务端口
BILIUP_PORT=3600
