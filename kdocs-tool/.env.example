# ── 夸克网盘分享页总大小自动抓取（可选，默认零配置）──
# 粘贴文本含夸克分享链接时，kdocs-tool 会自动调夸克接口递归求和总大小（接口无需签名，仅需登录 Cookie）。
# Cookie 来源优先级：
#   1) 默认：只读复用 netdisk-hub 的 data/store.json 中已登录的夸克 Cookie（netdisk-hub 已用 Playwright 登录即零配置）
#   2) 或在此自填夸克网页端 Cookie（最优先，覆盖上面的复用）：
QUARK_COOKIE=
# 若 netdisk-hub 不在默认路径 E:/工作空间/netdisk-hub，可指定其目录（仅方案1需要）：
# NETDISK_HUB_DIR=E:/工作空间/netdisk-hub
